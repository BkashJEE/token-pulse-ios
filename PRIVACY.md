# Privacy

Token Pulse for iOS connects only to the server URL configured by the user. The read access key is stored in the iOS Keychain using a device-only accessibility class. The app does not request the desktop write credential.

The current source includes no analytics, advertising SDK, tracking, location, contacts, camera, or microphone access. App Store privacy disclosures must be reviewed again if the production service or SDK set changes.
